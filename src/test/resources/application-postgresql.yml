spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/circle_test?currentSchema=sample
    username: ubuntu
    password:

flyway:
  enabled: false

mybatis:
  mapper-locations: mybatis/postgresql/*.xml
