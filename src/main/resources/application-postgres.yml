spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/testdb?currentSchema=sample
    username: shishamo
    password:

flyway:
  enabled: true
  locations: db/postgres
