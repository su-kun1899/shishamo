spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL
    username: sa
    password:
  h2:
    console:
      enabled: true
flyway:
  enabled: true
  locations: db/postgres